apply plugin: "war"

bootWar {
    mainClassName = "com.todo.demo.TodoDemoApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
